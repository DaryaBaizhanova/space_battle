name: C/C++ CI

on:
  push:
    branches: [ "test" ]
  pull_request:
    branches: [ "test" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: install cppunit
      run: sudo apt install -y libcppunit-dev
    - run: sudo apt install -y qtbase5-dev qt5-qmake cmake
    - run: qmake
    - run: make
    - run: sudo chmod u+x server
    - run: ./server
